{{- $installRhcl := index .Values "install-rhcl" }}
{{- if and .Values.kuadrant.create (not $installRhcl.enabled) }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.kuadrant.namespace }}
  labels:
    {{- include "rhcl.labels" . | nindent 4 }}
{{- end }}
