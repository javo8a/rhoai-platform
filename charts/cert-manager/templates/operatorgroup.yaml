apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: cert-manager-operator
  namespace: {{ .Values.operatorNamespace }}
spec:
  upgradeStrategy: Default
  targetNamespaces:
    - {{ .Values.operatorNamespace | quote }}
